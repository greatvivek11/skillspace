# Sample Data

Store only tiny, git-safe example files here.

Current seed files:

- `jobs_sample.csv`
- `resumes_sample.csv`
- `skills_seed.csv`

These are hand-authored starter examples so we can build and test the pipeline
without depending on external downloads during initial development.
